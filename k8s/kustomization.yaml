apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: ai-service

resources:
  - namespace.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml
  - hpa.yaml

images:
  - name: ghcr.io/ems-project/ai-service
    newTag: v1.0.0
