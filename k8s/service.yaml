apiVersion: v1
kind: Service
metadata:
  name: ai-service
  namespace: ai-service
spec:
  ports:
    - port: 80
      targetPort: 8081
      name: http
    - port: 9091
      targetPort: 9091
      name: metrics
  selector:
    app: ai-service
