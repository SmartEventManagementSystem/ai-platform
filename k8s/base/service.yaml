apiVersion: v1
kind: Service
metadata:
  name: ai-service
  namespace: ai-service
spec:
  type: ClusterIP
  ports:
    - port: 8081
      targetPort: 8081
      name: http
  selector:
    app: ai-service
